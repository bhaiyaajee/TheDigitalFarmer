<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
   

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AgricultureInfo"></activity>
        <activity android:name=".ShowProfile" />
        <activity android:name=".BuyerHistory" />
        <activity android:name=".FarmerHistory" />
        <activity android:name=".FinalSellCrop" />
        <activity android:name=".SellCrop" />
        <activity android:name=".BuyerBidding" />
        <activity android:name=".BuyCrop" />
        <activity android:name=".AddCrop" />
        <activity android:name=".BuyerScreen" />
        <activity android:name=".FarmerScreen" />
        <activity android:name=".OtpVerificationActivity" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".HeartActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".PasswordReset" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
